var registerTask = module.exports = function(name, packageUrl) {
    'use strict';

    if (!name || !packageUrl) {
        throw new Error('Usage: pas docker:register [name] [package-url]');
    }

    // TODO register here
};